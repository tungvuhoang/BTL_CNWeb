const QuestionCard = ({ content }) => {
  return (
    <div className="host-question-card">
      <h2 className="host-question-card__text">{content}</h2>
    </div>
  );
};

export default QuestionCard;
